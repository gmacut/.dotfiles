# TCSH ONLY!!! -- adds suport for current git branch indication in prompt
# should be registered with:
#     aliad precmd 'source this_file'
#

# line: `sh -c 'git branch --no-color 2> /dev/null' | sed -e '/^[^*]/d' -e 's/* \(.*\)/(\1)/'` 
# should be added to prompt

set prompt="\
%B%n%b@%U%m%u %S%/%s\ `sh -c 'git branch --no-color 2> /dev/null' | sed -e '/^[^*]/d' -e 's/* \(.*\)/(\1)/'` \
%# "
